package rag.intents;

public class RuleIntentDetector implements IntentDetector {
    @Override
    public Intent detect(String question) {
        // TODO rule-based intent detection
        return Intent.UNKNOWN;
    }
}
